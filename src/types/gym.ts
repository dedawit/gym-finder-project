export interface GymCardProps {
  name: string;
  image: string;
  rating: number;
  reviewCount: number;
  address: string;
  distance: string;
  features: string[];
  hours: string;
  price: number;
  phone?: string;
}
